/*
 * HUD module
 * Dependency: constants, tileindex
 */
define(['lodash','phaser', 'modules/constants'], function(_, Phaser, Constants) {
  var _entityTypes = {};

  _entityTypes.BaseSprite = function() {
  	
  };

  
  return {
  	create: function(typeString) {

  	}
  };
});